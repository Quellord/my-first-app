const items =["apple","banana","mango","fig"]

const Main = (props) => {
  return (
    <div className="main">
     { props.children}
     {items.map =(item)
     }
      
    </div>
  )
}

export default Main